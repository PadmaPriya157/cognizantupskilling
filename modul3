// 31 . Basic JDBC Connection
import java.util.*;
import java.sql.*;
public class Main
{
	public static void main(String[] args) {
		System.out.println("Hello World");
		try{
		Connection con=DriverManager.getConnection("https","username","password");
		PreparedStatement stmt=con.prepareStatement("select * from Students");
	    ResultSet res=stmt.executeQuery();
	    while(res.next())
	    {
	        String name=res.getString("name");
	        System.out.println(name);
	    }
	   // System.out.println("asdfghj");
	    con.close();
	    stmt.close();
		    
		}
		catch(Exception e)
		{
		    System.out.println("error");
		}
		
	}
}

// 32 . insert
import java.util.*;
import java.sql.*;
public class Main
{
	public static void main(String[] args) {
		System.out.println("Hello World");
		try{
		Connection con=DriverManager.getConnection("https","username","password");
	    String query="insert into sttudent values(?,?)";
		PreparedStatement stmt=con.prepareStatement(query);
	    stmt.setInt(1,1);
	    stmt.setString(2,"priya");
	    stmt.executeUpdate();
	    System.out.println("inserted sucessfully")
	  
	    con.close();
	    stmt.close();
		    
		}
		catch(Exception e)
		{
		    System.out.println("error");
		}
		
	}
}

// 32 .update
import java.util.*;
import java.sql.*;
public class Main
{
	public static void main(String[] args) {
		System.out.println("Hello World");
		try{
		Connection con=DriverManager.getConnection("https","username","password");
	    String query="update sttudent set name=? where id=?";
		PreparedStatement stmt=con.prepareStatement(query);
	    stmt.setString(1,"priya");
	    stmt.setInt(2,1);
	    stmt.executeUpdate();
	    System.out.println("updated sucessfully")
	  
	    con.close();
	    stmt.close();
		    
		}
		catch(Exception e)
		{
		    System.out.println("error");
		}
		
	}
}
35. TCP Client-Server Chat
package jakarta.servlet;

//Server.java
import java.io.*;
import java.net.*;
import java.util.Scanner;

public class Server {
 public static void main(String[] args) {
     try {
        ServerSocket  serversocket=new ServerSocket(3210);
        System.out.println("server is waiting");
        Socket socket=serversocket.accept();
        System.out.println("client connected");
        
        BufferedReader in=new BufferedReader(new InputStreamReader(socket.getInputStream()));
        PrintWriter out=new PrintWriter(socket.getOutputStream(),true);
        Scanner scn=new Scanner(System.in);
        while(true)
        {
        	String clientmsg=in.readLine();
        	System.out.println("Client"+clientmsg);
        	if(clientmsg.equals("exit"))
        		break;
        	System.out.println("Server:");
        	String servermsg=scn.nextLine();
        	out.println(servermsg);

        	
        }
        
        socket.close();
        serversocket.close();

     } catch (Exception e) {
         System.out.println(e);
     }
 }
}
package jakarta.servlet;

//Client.java
import java.io.*;
import java.net.*;
import java.util.Scanner;

public class Client {
 public static void main(String[] args) {
     try {
        Socket socket=new Socket("localhost",321pri0);
        BufferedReader in=new BufferedReader(new InputStreamReader(socket.getInputStream()));
        PrintWriter out=new PrintWriter(socket.getOutputStream(),true);
        Scanner scn=new Scanner(System.in);
        while(true)
        {
        	System.out.println("Client:");
        	String clientmsg=scn.nextLine();
        	out.println(clientmsg);
        	if (clientmsg.equalsIgnoreCase("exit"))
        		break;
        	String servermsg=in.readLine();
        	System.out.println("Server"+servermsg);
        	if(servermsg.equals("exit"))
        		break;

        	
        }
        socket.close();

     } catch (Exception e) {
         System.out.println(e);
     }
 }
}
36 . HTTP Client API (Java 11+)
package jakarta.servlet;

import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

////HttpClientExample.java
//
//import java.net.URI;
//import java.net.http.HttpClient;
//import java.net.http.HttpRequest;
//import java.net.http.HttpResponse;

public class Client {

 public static void main(String[] args) {

     try {

        HttpClient client=HttpClient.newHttpClient();
        HttpRequest request= HttpRequest.newBuilder().uri(URI.create("https://api.github.com/users/octocat"))
        		.GET()
        		.build();
        HttpResponse<String>response=client.send(request,HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());

     } catch (Exception e) {
         System.out.println(e);
     }
 }
}

37. Using javap to Inspect Bytecode
package jakarta.servlet;

//Server.java
import java.io.*;
import java.net.*;
import java.util.Scanner;

public class Server {
 public static void main(String[] args) {
	 

	 System.out.println(1+2);
 }
}
C:\Users\priya\eclipse-workspace\JAVA\bin>javap -c jakarta.servlet.Server
Compiled from "Server.java"
public class jakarta.servlet.Server {
  public jakarta.servlet.Server();
    Code:
       0: aload_0
       1: invokespecial #8                  // Method java/lang/Object."<init>":()V
       4: return
 public static void main(java.lang.String[]);
    Code:
       0: getstatic     #16                 // Field java/lang/System.out:Ljava/io/PrintStream;
       3: iconst_3
       4: invokevirtual #22                 // Method java/io/PrintStream.println:(I)V      
       7: return
}
38. Decompile a Class File
https://www.javadecompilers.com/?utm_source=chatgpt.com
decompiler url

39. Reflection in Java
package jakarta.servlet;

import java.lang.reflect.Constructor;
import java.lang.reflect.Method;
public class Server {
 public static void main(String[] args) throws Exception {
	 

	Class<?>cls=Class.forName("jakarta.servlet.Client");
	Method meth[]=cls.getDeclaredMethods();
	System.out.println("------------------methods--------------");
	for(Method m:meth)
		System.out.println(m);
	
	Object obj=cls.getDeclaredConstructor().newInstance();
	Method m=cls.getDeclaredMethod("show");
	
	System.out.print("INvoke");
	m.invoke(obj);
	
	
 }
}
package jakarta.servlet;

public class Client {
	public Client()
	{
		
	}
	public void show()
	{
		System.out.println("Hello");
	}
}
